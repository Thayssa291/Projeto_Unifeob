@model Contribuinte_Model
@{
    ViewData["Title"] = "Tela de confirmação";
    var mensagemApagar = ViewBag.MensagemApagar;
}
<div class="mb-3 text-center">
    <h1>Deseja excluir o contribuinte "@Model.RazaoSocial"?</h1>
    </br>
    <a class="btn btn-danger" asp-controller="Contribuinte" asp-action="Apagar" asp-route-id="@Model.Id">Sim</a>
    <a class="btn btn-primary" asp-controller="Contribuinte" asp-action="ListagemContrib">Não</a>
    @if (!string.IsNullOrEmpty(mensagemApagar)) {
        <div class="text-danger">@mensagemApagar</div>
    }
</div>
